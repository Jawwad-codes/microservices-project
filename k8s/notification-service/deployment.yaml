apiVersion: apps/v1
kind: Deployment

metadata:
  name: notification-service
  namespace: microservices
  labels:
    app: notification-service

spec:
  replicas: 2

  selector:
    matchLabels:
      app: notification-service

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1

  template:
    metadata:
      labels:
        app: notification-service

    spec:
      containers:
        - name: notification-service
          image: 792705600668.dkr.ecr.ap-south-1.amazonaws.com/notification-service:latest
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 4005
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: notification-secret

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          readinessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 20
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 30
            periodSeconds: 10