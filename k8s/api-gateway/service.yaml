apiVersion: v1
kind: Service

metadata:
  name: api-gateway-service
  namespace: microservices
  labels:
    app: api-gateway

spec:
  type: NodePort

  selector:
    app: api-gateway

  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP